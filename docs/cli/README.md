# CLI Reference

Generated via `cargo xtask doc cli`. The Markdown file `docs/cli/gat.md` (and its man page counterpart `docs/man/gat.1`) is safe to commit so agents can read a deterministic snapshot of the latest CLI help.
