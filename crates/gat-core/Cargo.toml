[package]
name = "gat-core"
version = "0.5.3"
edition = "2021"

[features]
default = []
wasm = []

[dependencies]
anyhow = "1.0"
faer = "0.17"
once_cell = "1"
petgraph = "0.6"
serde = { version = "1.0", features = ["derive"] }

[dev-dependencies]
serde_json = "1.0"
